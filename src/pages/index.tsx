import { Button } from 'antd';

const Home: React.FC = () => {
    return (
        <div>
            Welcome to Next.js!
            <Button>dsfs</Button>
        </div>
    );
};

export default Home;
